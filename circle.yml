general:
  branches:
    only:
      - v2.0.2
      - v2.1.0

dependencies:
  override:
    - npm install -g bower
    - npm install -g ember-cli
    - npm install
    - bower install

test:
  override:
    - ember test --config-file=testem-circle.json
